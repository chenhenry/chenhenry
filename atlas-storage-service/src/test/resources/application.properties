#application related

#code related
hdfs.httpUrl=http://10.5.130.86:50070/webhdfs/v1/
hdfs.user=hdfs
import.isMutilThread=true
hdfs.testUrl=hdfs://10.5.130.86:8020/tmp/

#database
spring.datasource.url=jdbc:h2:mem:storage;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# Show or not log for each sql query
spring.jpa.show-sql = true
# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = create-drop
# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
# stripped before adding them to the entity manager)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.H2Dialect

#data connect service
dataconnector.service.url=http://appatlas3:8081/dataconnector
